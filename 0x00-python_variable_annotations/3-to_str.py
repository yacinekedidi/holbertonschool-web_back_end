#!/usr/bin/env python3
"""[simple module that contains a method]"""


def to_str(n: float) -> str:
    """[takes a float n as argument and
    returns the string representation of the float.]

    Args:
        n (float): [a float number]

    Returns:
        str: [the string representation of n]
    """
    return str(n)
